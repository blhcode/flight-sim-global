# GitHub Actions deploy (optional)

Copy to `.github/workflows/deploy.yml` after running `gh auth refresh -s workflow`.

See the workflow file in this repo's git history, or use manual gh-pages deploy (see README).
